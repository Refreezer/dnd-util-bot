---
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  skip: true
run:
  image: hub.docker.com/repository/docker/refreezer/dnd-util-bot:latest
  persistenceMount: /data
