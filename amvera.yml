---
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: false
run:
  persistenceMount: /data
  containerPort: 80
  command: go mod download & go build -C cmd -o ../dnd-util-bot & chmod +x dnd-util-bot
  image: dnd-util-bot
